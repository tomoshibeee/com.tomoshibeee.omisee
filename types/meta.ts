export type MetaData = {
  site_id: string, 
  site_no: number,
  slug: string;
  name: string;
  tel: string;
  email: string;
  postalCode?: string;
  address: string;
  bldg?: string;
  access: string;
  description?: string;
};
