export type SiteSocialLink = {
    id: string;
    site_id: string;
    type_id: string; // m_social_types.id
    url: string;
    display_order: number;
    created_at: string;
    updated_at: string;
};

